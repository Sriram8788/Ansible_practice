-
 name: playbook on current loops concept
 hosts: all
 tasks:
 - name: remove applications
   yum: 
    state: absent
    name: "{{ item }}"
   loop:
   - docker
   - net-tools

 - name: stops services
   services:
    name: "{{ item }}"
    state: stopped
   loop:
   - httpd

-
 name: playbook on current loops concept
 hosts: all
 tasks:
 - name: remove applications
   yum: 
    state: absent
    name: "{{ item }}"
   loop:
   - docker
   - net-tools

 - name: stops services
   service:
    name: "{{ item }}"
    state: stopped
   loop:
   - httpd
